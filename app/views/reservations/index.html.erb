<%= form_with (model:@rooms, @reservations) do |f| %>
<div class="reservation-list">
 <h4 class="pb-4">予約済みルーム一覧</h4>
 <table>
  <thead>
   <tr align="center">
    <th>ルーム画像</th>
    <th>ルーム名</th>
    <th>ルーム紹介</th>
    <th>料金</th>
    <th>作成日</th>
   </tr>
  </thead>
  <tbody>
  
   <tr>
    <td><%= room.name %></td>
    <td><%= room.name %></td>
    <td><%= room.introduction %></td>
    <td><%= reservation.price %></td>
    <td><%= reservation.created_at.to_s(:datetime_jp) %></td>
   </tr>
 
  </tbody>
 </table>
</div>
<% end %>