<div class="top">
 <div class="top-main">
  <div class="text-area">
   <div class="form-area">
    <div class="search-area-m">
     Potepan Shareでまだ体験したことのない旅をしよう
     <div class="search-box2">
        <%= form_with url: search_path, method: :get, local: true do |f| %>
         <%= f.text_field :search, placeholder: "エリア", :class => 'search-m' %>
         <%= f.submit "検索", class: "btn btn-primary" %>
        <% end %>
     </div> 
    </div>     
   </div>
  </div>
 </div>
</div>